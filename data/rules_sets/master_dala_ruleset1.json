{
  "name": "master_dala_ruleset1",
  "desc": "",
  "enabled": false,
  "params": [],
  "dev_options": null,
  "rule_groups": [
    {
      "rules": [
        {
          "desc": "We a need the tranids as strings with the OID mappings. Use JS to perform that functions.",
          "type": "mapreduce",
          "script_options": {
            "collection": "temp_db"
          },
          "save_options": {
            "temp_db": true
          },
          "scripts": [
            "{}",
            "function() {}",
            "function(key, value) {}"
          ]
        }
      ]
    }
  ]
}